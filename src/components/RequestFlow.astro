<figure class="request-flow" aria-labelledby="request-flow-title" aria-describedby="request-flow-description">
  <figcaption id="request-flow-title">How it works</figcaption>
  <p id="request-flow-description" class="sr-only">Your coding agent sends a request through Torana and its approved plugin pipeline to your configured provider. The response returns through Torana to your agent. The agent and Torana run on your machine; a remote provider is shown here.</p>
  <div class="machine-boundary">
    <p class="boundary-label">On your machine</p>
    <div class="flow-node agent-node">
      <strong>Your coding agent</strong>
      <span>Your tools. Your workflow.</span>
    </div>
    <div class="connector" aria-hidden="true">
      <svg viewBox="0 0 48 40" width="48" height="40" fill="none"><path d="M16 2v34m-5-5 5 5 5-5M32 38V4m-5 5 5-5 5 5" /></svg>
    </div>
    <div class="flow-node proxy-node">
      <strong>Torana</strong>
      <span>Your approved plugin pipeline</span>
      <ul aria-label="Example plugin capabilities"><li>Observe</li><li>Apply policy</li><li>Transform</li></ul>
    </div>
  </div>
  <div class="connector provider-connector" aria-hidden="true">
    <svg viewBox="0 0 48 40" width="48" height="40" fill="none"><path d="M16 2v34m-5-5 5 5 5-5M32 38V4m-5 5 5-5 5 5" /></svg>
    <span>Request / response</span>
  </div>
  <div class="flow-node provider-node">
    <strong>Your model provider</strong>
    <span>The endpoint you configured</span>
  </div>
  <p class="flow-note">Remote provider shown. You can also connect a supported local model endpoint.</p>
</figure>

<style>
  /* Hallmark · scoped request diagram · existing teal tokens
   * pre-emit critique: P5 H4 E5 S5 R5 V4 · no added motion or browser chrome
   * rendered: local/provider boundary fits 320–1920px · text contrast >= 6.79:1
   */
  .request-flow { margin: 0; min-width: 0; width: 100%; }
  figcaption { color: var(--muted); margin-bottom: var(--space-4); font-size: .875rem; }
  .machine-boundary { border: 1px solid var(--rule-strong); border-radius: var(--radius); padding: var(--space-5); }
  .boundary-label { margin: 0 0 var(--space-4); color: var(--muted); font-size: .875rem; }
  .flow-node { display: flex; flex-direction: column; gap: var(--space-1); min-width: 0; }
  .flow-node strong { font-family: var(--font-display); font-size: 1.25rem; line-height: 1.2; }
  .flow-node span { color: var(--ink-2); font-size: .875rem; }
  .proxy-node { background: var(--paper-2); padding: var(--space-4); border-radius: var(--radius); }
  .proxy-node strong { color: var(--accent); }
  .proxy-node ul { display: flex; flex-wrap: wrap; gap: var(--space-2) var(--space-3); list-style: none; margin: var(--space-3) 0 0; padding: 0; color: var(--ink-2); font-size: .875rem; }
  /* Equal side tracks keep the arrows themselves centered, not arrows + label. */
  .connector { display: grid; grid-template-columns: minmax(0, 1fr) var(--space-7) minmax(0, 1fr); align-items: center; min-height: var(--space-7); color: var(--muted); }
  .connector svg { grid-column: 2; justify-self: center; stroke: var(--accent); stroke-width: 1.5; stroke-linecap: round; stroke-linejoin: round; }
  .provider-connector { font-size: .875rem; }
  .provider-connector span { grid-column: 3; min-width: 0; padding-inline-start: var(--space-2); line-height: 1.4; }
  .provider-node { border: 1px solid var(--rule-strong); border-radius: var(--radius); padding: var(--space-4) var(--space-5); }
  .flow-note { color: var(--muted); font-size: .875rem; margin: var(--space-3) 0 0; }
</style>
